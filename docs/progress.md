# 진행 상황

## 완료

### 마일스톤 1. 프로젝트 기반 구축

- Python 패키지 구조 생성
- JSON 설정 로더 추가
- 민감 정보 제외 예시 설정 추가
- 기본 CLI 추가
- 기본 로깅 추가
- SQLite DB 초기화 명령 추가

### 마일스톤 2. 전략 엔진 구현

- ATR 20일 계산
- 20일 최고가 돌파 진입 신호
- 10일 최저가 이탈 청산 신호
- 마지막 진입가 기준 `-2 ATR` 손절 신호
- `+0.5 ATR` 추가 진입 신호
- 종목당 최대 4 Unit 제한
- 총 자산 1% 리스크 기반 Unit Size 계산

### 마일스톤 3. 상태 저장소 구현

- SQLite 저장소 추가
- 계좌 스냅샷 테이블
- 유니버스 이력 테이블
- 포지션 테이블
- 주문 후보 테이블
- 승인 이력 테이블
- 주문 이벤트 테이블
- 보고서 테이블
- 주문 후보 멱등성 키 중복 방지

### 마일스톤 4. 한국투자증권 API 연동 초안

- 인증 토큰 발급 요청 생성
- 해외주식 일봉 조회 요청 생성
- 해외주식 잔고 조회 요청 생성
- 해외주식 주문 요청 생성
- 모의투자 TR ID 변환
- 가짜 전송기를 이용한 요청 생성 테스트

### 마일스톤 5. 텔레그램 승인 흐름 초안

- 승인 요청 메시지 생성
- 승인, 거절, 보류 응답 파싱
- 장 종료 보고 메시지 생성
- 텔레그램 메시지 전송 클라이언트
- 가짜 전송기를 이용한 전송 테스트
- 실제 텔레그램 테스트 메시지 발송 성공
- 주문 후보가 있을 때 승인 요청을 보내는 명령 추가
- 텔레그램 응답을 읽고 승인 상태를 DB에 저장하는 명령 추가
- 텔레그램 update offset 저장으로 중복 응답 처리 방지

### 마일스톤 6. 주문 오케스트레이션 초안

- 전략 신호를 주문 후보로 변환
- 주문 후보 저장
- 주문 후보 중복 방지
- 승인된 후보만 실행 대상으로 전달
- 드라이런 실행 결과 기록
- 당일 계획 생성 명령 추가
- 미국 동부시간 기준 기본 거래일 계산
- 승인된 주문 후보만 드라이런 실행 이벤트로 기록하는 명령 추가
- 개장 전 통합 워크플로우 `pre-market` 추가
- 장 종료 통합 워크플로우 `market-close` 추가
- 주말 실행 자동 스킵 안전장치 추가
- 실행 이벤트 멱등성 키 추가
- 승인 요청 중복 전송 방지 상태 추가

### 마일스톤 7. 장 종료 보고 초안

- 해외주식 체결내역 조회 API 래퍼 추가
- 해외주식 미체결내역 조회 API 래퍼 추가
- 장 종료 보고서 생성
- 장 종료 보고서 DB 저장
- 옵션 기반 텔레그램 전송 지원

### 마일스톤 9. 캘린더와 운영 안정화 초안

- 미국 동부시간 기준 기본 거래일 계산
- 주말 거래일 여부 판정
- NYSE 주요 정기 휴장일 판정
- 다음 거래일 계산
- `--force` 옵션으로 테스트 실행 허용
- 조기폐장일 자동 반영은 미구현

### 안전장치 추가

- 실주문 실행기 안전 레이어 추가
- `app.env=live`와 `broker.mode=live`가 동시에 만족될 때만 실주문 실행기 생성 가능
- 현재 `config.local.json`은 `broker.mode=live`, `app.env=dry-run`이라 실주문 잠김
- `safety-check` 명령 추가
- `status` 명령 추가
- 주문 직전 재검증 명령 추가
- `execute-approved --validate`로 검증 통과 후보만 드라이런 실행 지원
- 주문 직전 최종 리뷰 메시지 생성
- `validate-approved --send` 텔레그램 발송 옵션 추가

### 운영 스크립트

- `scripts/pre_market.ps1`
- `scripts/market_close.ps1`
- `scripts/safety_check.ps1`

### 리허설 및 운영 조회

- 네트워크 없는 로컬 승인/드라이런 리허설 명령 추가
- 주문 후보, 승인, 실행 상태 조회 명령 추가
- 로컬 DB 백업 명령 추가
- 장 종료 보고서에 로컬 실행 이벤트 요약 추가
- 운영 명령 목록 출력 명령 추가
- 주문 후보 payload에 주문용 거래소 코드 저장

### 백테스트 초안

- KIS 최근 일봉 기반 유니버스 백테스트 명령 추가
- 종가 체결 가정
- 수수료/세금/환율/파킹 ETF 제외
- 현재 유니버스 고정
- JSON 결과 저장 옵션 추가

### 한국투자증권 API 연결 확인

- 모의투자 API 토큰 발급 성공
- 실전 API 토큰 발급 성공
- 실전 해외주식 잔고 조회 성공
- 현재 조회 기준 보유 해외주식 0개
- 실전 해외주식 일봉 조회 성공
- 기본 유니버스 10개 전략 신호 계산 성공
- KIS 접근토큰 로컬 캐시 추가

### 현재 2026-06-11 계획 생성 결과

- 스냅샷 저장 수: 4
- 포지션 수: 0
- 주문 후보 수: 0
- 승인 메시지: 주문 후보 없음
- 장 종료 보고서 수: 1
- 승인 수: 0
- 실행 이벤트 수: 0

## 검증

```text
python -m unittest discover -s tests
Ran 25 tests
OK
```

```text
python -m turtle_invest doctor --config config.example.json
turtle-invest runtime OK
```

```text
config.local.json 기준:
- Telegram sendMessage 성공
- KIS paper token 발급 성공
- KIS live overseas balance 조회 성공
- KIS live daily price 조회 성공
- plan-day 실행 성공
- request-approval 실행 성공, 후보 없음으로 미전송
- close-report 실행 성공, 체결/미체결 0건
- execute-approved 실행 성공, 승인 후보 0건
- pre-market 통합 워크플로우 실행 성공
- market-close 통합 워크플로우 실행 성공
- 주말 스킵 동작 확인
- NYSE 휴장일 스킵 동작 확인
- safety-check 실행 성공
- status 실행 성공
- rehearse-local 실행 성공
- orders 실행 성공
- backup-db 실행 성공
- commands 실행 성공
- 주문용 거래소 코드 보강 후 pre-market 실행 성공
- backtest 실행 성공
- validate-approved 실행 성공
- execute-approved --validate 실행 성공
```

## 다음에 사용자 정보가 필요한 항목

- 한국투자증권 모의투자 API 앱 키와 앱 시크릿
- 한국투자증권 모의투자 계좌번호와 상품 코드
- 텔레그램 봇 토큰
- 텔레그램 채팅 ID
- 사용할 주문 시간대: 정규장만, 프리마켓 포함, 애프터마켓 포함 중 선택
- 사용할 주문 유형: 지정가, 시장가, 기타 조건 주문 중 선택
